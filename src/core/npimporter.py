# This simple file allows the user to select cupy or numpy as the numerical processor
# Importing either as np here allows one import elsewhere using `import np from npimporter`
#import cupy as np
import numpy as np
